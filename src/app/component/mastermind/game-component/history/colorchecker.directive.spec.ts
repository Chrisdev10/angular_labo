import { ColorcheckerDirective } from './colorchecker.directive';

describe('ColorcheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorcheckerDirective();
    expect(directive).toBeTruthy();
  });
});
