import { SamplerFillerDirective } from './sampler-filler.directive';

describe('SamplerFillerDirective', () => {
  it('should create an instance', () => {
    const directive = new SamplerFillerDirective();
    expect(directive).toBeTruthy();
  });
});
